{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ],
    "buildCommand": "npm run build",
    "outputDirectory": "frontend/dist",
    "framework": "vite"
}